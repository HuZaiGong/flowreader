# keystore.properties.example — release 签名配置模板
#
# 用法：cp keystore.properties.example keystore.properties，改成自己的密钥库信息。
# `keystore.properties` 与 `*.jks` / `*.keystore` 均已在 .gitignore 中，不会进版本控制。
#
# 这个文件存在时，`./gradlew assembleRelease` 用下面配置的正式密钥签名；
# 不存在时（CI、新克隆、没有密钥库的人）自动回退到 debug 签名，
# 保证 assembleDebug 和单元测试照常可跑。见 app/build.gradle.kts 的 signingConfigs 块。
#
# 生成一个密钥库（有效期 ~27 年，放在仓库外面）：
#   keytool -genkeypair -v -keystore ~/flowreader-release.jks \
#     -alias flowreader -keyalg RSA -keysize 2048 -validity 10000
#
# 验证产物确实用它签了名（Signer #1 的 DN 应该是你自己的，不是 CN=Android Debug）：
#   apksigner verify --print-certs app/build/outputs/apk/release/app-release.apk
#
# 注意：一旦某个版本以某密钥库发布出去，后续版本必须用同一个密钥库签名，
# 否则用户无法覆盖安装升级。请备份好这个 .jks 和密码。

storeFile=/absolute/path/to/flowreader-release.jks
storePassword=your-store-password
keyAlias=flowreader
keyPassword=your-key-password
