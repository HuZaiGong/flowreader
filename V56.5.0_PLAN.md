# v56.5.0 起草计划

状态：**草稿，未实施**。本文件是本地起草，不提交到 main，也不推送。
基线：`5f437af`（v56.4.4，已发布）。

> 归档说明：该计划后来以 v56.5.0 的两个 wip 提交完成，并已补入 [CHANGELOG.md](CHANGELOG.md)。以下内容保留为实施记录；当前版本事实以 [ARCHITECTURE.md](ARCHITECTURE.md) 和 [ROADMAP.md](ROADMAP.md) 为准。

---

## 范围与取舍

本版含两类内容：**修复**（遗留缺陷）与**新功能**（更多预设配色、自导入背景图）。

v56.4.4 收尾时我向你报了两个遗留缺陷。动手前重新核查，发现其中一个的真实规模远超我当时的说法，因此这里先把边界划清楚。

**我当时说错的地方**：我说 `StatsScreen.kt:41` 硬编码标题「会让 v53 的语言切换在这个标题上失效」，听起来像孤例。实际全仓扫描 `app/src/main/java/com/flowreader/app/ui/` 下有 **58 处**中文硬编码，分布在 10 个文件里：

| 文件 | 处数 |
|------|------|
| `screens/reader/components/Dialogs.kt` | 15 |
| `screens/bookdetail/BookDetailScreen.kt` | 11 |
| `screens/stats/StatsScreen.kt` | 10 |
| `screens/wheel/WheelScreen.kt` | 6 |
| `screens/reader/components/ReaderControls.kt` | 5 |
| `screens/reader/components/BookmarksDialog.kt` | 4 |
| `screens/reader/components/ReaderSettingsSheet.kt` | 3 |
| `screens/reader/components/ChapterListDialog.kt` | 2 |
| `screens/reader/components/PdfViewer.kt` | 1 |
| `screens/reader/components/PagedReader.kt` | 1 |

其中**两处是顶栏标题**，是切换语言后用户第一眼就会看到的：`StatsScreen.kt:41` 的「阅读统计」和 `WheelScreen.kt:53` 的「决策转盘」。

`LibraryScreen.kt:202` 用的是 `stringResource(R.string.library_title)`，那是正确写法的现成对照。

---

## 任务

### 1. 顶栏标题国际化（必做，影响最直接）

`StatsScreen.kt:41` → `stringResource(R.string.stats_title)`。资源已齐备，**9 种语言全都有**（zh 统计 / en Stats / ja 統計 / ko 통계 / de Statistiken / es Estadísticas / fr Statistiques / pt Estatísticas / ru Статистика），只需改调用点，无需新增资源。

`WheelScreen.kt:53` → 已核实 `wheel_title` **不存在**，需在 9 个 `values*/strings.xml` 各补一条后再改调用点。

### 2. 深色模式状态栏图标 —— 撤销，这不是缺陷

**我在 v56.4.4 收尾时报错了这一项，实施前核查后撤销。**

我当时说 `themes.xml:6` 硬编码 `android:windowLightStatusBar=true` 且没有 `values-night/`，会导致深色模式下深色图标配深色背景。读代码后确认不成立：

`FlowTheme.kt:56-63` 已经在 `SideEffect` 里做了运行时设置：

```kotlin
WindowCompat.getInsetsController(window, view).apply {
    isAppearanceLightStatusBars = !darkTheme
    isAppearanceLightNavigationBars = !darkTheme
}
```

而且它比 `values-night/` 资源限定符**更正确**：`darkTheme` 来自 `AppThemeMode.isDark()`，包含用户在应用内的选择；资源限定符只看系统设置，用户在应用内选 DARK 而系统是浅色时会失效。运行时设置覆盖 XML 主题属性，所以 `themes.xml` 里那一行只在 Activity 创建到首帧合成之间短暂生效。

`FlowTheme` 在 `Navigation.kt:113` 包住整个 NavHost，所有界面都在其之下，已验证无遗漏。

**结论：不改。** 加 `values-night/themes.xml` 属于无效改动，还会增加一处与运行时逻辑重复、可能不一致的配置。

### 3. 其余 56 处硬编码（本版只做一部分）

不一次做完，理由：58 处要新增大量字符串资源 × 9 种语言，改动面巨大且与前两项风险等级不同，混在一个版本里既难 review 也难回滚。

本版建议只做**用户可见层级最高**的一批：`BookmarksDialog`、`ChapterListDialog`、`ReaderControls` 的菜单项（共 11 处），都是阅读器主路径上的入口文字。

`Dialogs.kt`(15) 和 `BookDetailScreen.kt`(11) 留到 v56.5.1，单独一版做完并补测试。

### 4. 新增预设阅读配色（本次新加需求）

现状：`ReaderPalettes` 有 12 套，每套是 4 个 ARGB 长整数（背景／正文／次要文字／选区），全部由 `ReaderPaletteContrastTest` 做 WCAG AA 正文对比度断言。

新增 6 套，凑满 18：

| 建议 id | 名称 | 深色 | 定位 |
|---------|------|------|------|
| `SOLARIZED_LIGHT` | 曝光浅 | 否 | 经典程序员配色，低饱和暖底 |
| `SOLARIZED_DARK` | 曝光深 | 是 | 同上深色版 |
| `NORD` | 极地 | 是 | 冷蓝灰，低对比但仍需过 AA |
| `GRUVBOX` | 复古暖 | 是 | 暖棕底，护眼向 |
| `ROSE_QUARTZ` | 石英粉 | 否 | 浅粉底，低蓝光 |
| `FOREST` | 深林 | 是 | 深绿底，比现有 GREEN 更暗 |

**必须同步改动的三处**（漏一处就挂）：

1. `ReaderPaletteId` 枚举加 6 项（在 `:domain`）。
2. `ReaderPalettes` 加 6 个值 + 追加进 `all` 列表（在 `:core`）。
3. **`ReaderPaletteContrastTest:25` 硬编码 `assertEquals(12, ReaderPalettes.all.size)`** —— 必须改成 18。该测试还有 `assertEquals(ReaderPaletteId.entries.size, ReaderPalettes.all.size)`，会自动抓住枚举与表不同步。

配色值要在实施时逐一验算 AA 对比度（正文 ≥ 4.5:1），不能凭观感填。`ColorContrast.meetsAaBodyText` 是现成的验算函数，写完直接跑测试即可。

新增的 6 个 `ReaderPaletteId` 会让 `fromStoredName` 的迁移分支变长，但不需要改逻辑——它是 `entries.firstOrNull { it.name == raw }`，新枚举值自动可解析。

### 5. 自导入图片作为阅读背景（本次新加需求，风险最高）

**现成先例**：`SettingsViewModel.kt:301-312` 的自定义字体导入——落盘到 `context.filesDir/fonts/`，把绝对路径存进 DataStore（`CUSTOM_FONT_PATH`）。背景图照这个模式做，落到 `filesDir/backgrounds/`。

接入点很单一，这是有利条件：阅读背景目前只有 `ReaderScreen.kt:165` 一处 `.background(palette.background)`。

**必须解决的问题，按严重程度排**：

1. **对比度保证会失效**。这是最本质的冲突。整个阅读配色体系的正确性建立在「背景是纯色、可与文字算对比度」之上（`ReaderCustomTheme.resolve` 会在不达 AA 时静默回退）。一张任意图片没有单一背景色，`ColorContrast.meetsAaBodyText` 无从计算，正文可能落在图片亮部而完全不可读。
   - 对策：**强制叠加一层半透明遮罩**，遮罩色取自当前色板的 `backgroundArgb`，不透明度可调但设下限（建议 ≥ 0.5）。这样参与对比度计算的仍是那个纯色，AA 断言继续成立，图片只从遮罩后透出。遮罩不可关闭——这是保证可读性的结构性前提，不是偏好项。
2. **内存与解码上限**。项目对导入一律设限（`BookParser` 单图 24MB）。背景图要沿用同一量级的上限，并且必须按屏幕尺寸做降采样（`BitmapFactory.Options.inSampleSize`）后再落盘，否则一张 4000×3000 的图会常驻内存。
3. **分页模式下的重绘成本**。`PAGED` 模式在 `HorizontalPager` 里逐页测量渲染，背景图若每页重新解码会明显掉帧。图片应解码一次并复用（记在 ViewModel 或 remember 里），不能放在每页的 composable 内。
4. **持久化**：新增 DataStore 键 `reader_background_path` 与 `reader_background_dim`。`ReadingSettings` 加两个字段，走既有的唯一入口 `ReaderViewModel.updateReadingSettings(ReadingSettings)`——**不要新增单字段 setter**，这是 CLAUDE.md 明确的约束。
5. **安全**：图片经 SAF 选取，复制进应用私有目录后再使用，不保留 `content://` 长期引用；不给 FileProvider 授权（`file_paths.xml` 目前只授权 `share_cards/`，背景图不经它外传，保持现状）。
6. **`coverageSummary` 会受影响**：`ReadingSettings` 在 `:domain`，改的是已有文件不加新文件，分母不变。但如果为遮罩逻辑新建 `:core` 文件（比如 `ReaderBackground.kt`），**分母 +1，必须同时加测试文件**，否则比例下滑。遮罩合成逻辑应当写成 Compose 无关的纯函数以便测试，这与 `:core/util/` 既有的设计一致。

### 6. 门禁风险预判

- **`coverageSummary` 是文件数量比**（当前 47/62 = 75.8%，阈值 40%）。国际化与深色状态栏只改调用点，分母不变。但新增配色与背景图**可能新增 `:core` 文件与领域模型**，每加一个分母就 +1 —— 见第 5 节第 6 点，必须配套加测试。
- **空白字符门禁**对全仓 `.kt`/`.kts` 生效，新增 `values-night/themes.xml` 是 XML，不受这条约束，但仍要避免 tab。
- 新增字符串资源要 9 种语言同步，漏一种不会导致构建失败，但会在该语言下回退到中文——**这是静默失败，必须逐个核对**。

---

## 已完成（本次一并交付）

### `.gitignore` 修正

检查后发现四个问题，已修：

1. **`*.jar` 会误伤 `gradle/wrapper/gradle-wrapper.jar`**（最严重）。该文件此前靠 force-add 才在仓库里，一旦有人 `git rm --cached` 后重加就会被静默忽略，新克隆的仓库 `./gradlew` 无法 bootstrap。已加 `!gradle/wrapper/gradle-wrapper.jar` 反向规则并注明原因。
2. **`providers.yaml` 规则实际无效**。该文件已被跟踪并已在远端，而 `.gitignore` 对已跟踪文件不起作用——注释却写着「never commit real keys」，形成虚假安全感。已核实文件内的 `api_key` 是某公共免费中转站的共享占位值（此处不复述其字面量，见下）、第 34 行是注释示例，**不是密钥泄露**。规则改为 `providers.yaml.local`，并写明若要放真密钥必须先 `git rm --cached`。

   > v56.6.2 补记：上面这段结论本身是对的，但**把那个字面量抄进了正文**——于是这份计划文档成了整个仓库里唯一一处真正被提交的 key 字面量，且随 v56.5.0 一起进了公开的 `main`。已改为不复述。教训是：论证「这不是泄露」不需要引用值，引用了就把「不是泄露」变成了泄露。
3. **`.claude/settings.local.json` 只被全局 `~/.config/git/ignore` 挡住**，本仓库规则里没有。换机器 clone 就会暴露成未跟踪文件。已加入本仓库规则，含 `.tmp.*` 残留与 `worktrees/`。
4. **`HANDOFF.md`** 是开发过程笔记，此前一直以未跟踪状态漂在工作区。已加入忽略。

验证：`git check-ignore` 确认 wrapper jar 返回 rc=1（未被忽略，正确），其余四条规则均生效；工作区现只剩 `.gitignore` 一处改动。

`.opencode/` 无需处理——它自带 `.gitignore` 已覆盖 `node_modules` 等，且只跟踪了 2 个源文件。

### APK 体积告警排查（结论：假警报，非代码问题）

第 5 步跑完 `performanceBaseline` 报 `PERF-WARNING: debug APK grew 27473KB -> 28536KB (1063KB)`，而同一次 release 只涨 8KB。这个不对称不能放过——三个小类加十来条字符串不可能只在 debug 里多出 1MB。

查证过程与结论：

1. **依赖没变**：`git diff 5f437af..工作区 -- '*.gradle.kts' 'gradle/*' 'gradle.properties'` 为空，排除新库。
2. **旧基线可复现**：在 `5f437af`（v56.4.4）开 worktree 干净构建，得 **27473.9 KB**，与基线记录的 27473 完全吻合。所以那个数字是真的，增长也是真的，不是「基线不可信」。
3. **未压缩内容只多 42.8 KB**（dex +34.6，`resources.arsc` +8.2），压缩后只多 22 KB。也就是说这 1MB 既不在代码里也不在资源里。
4. **1MB 在 zip 结构里**：逐条目分析局部头偏移，条目间填充从 73.7 KB（220 处）涨到 **1114.4 KB（226 处）**，其中单独一处 **933.8 KB 空洞压在 `DebugProbesKt.bin` 前**（紧跟 `classes9.dex`）。这是 AGP 增量打包（zipflinger）的行为：替换 dex 时不重写整个归档，被换掉的旧条目原地留成空洞。本次会话反复重建十余次，空洞累积。
5. **干净重建验证**：`clean assembleDebug` 后当前工作区为 **27482.0 KB**，**真实增量 +8.1 KB**，与 release 的 +8 KB 一致。重跑 `performanceBaseline --rerun-tasks` 后 `PERF-WARNING` 消失。

基线已按干净构建重记为 `debug.kb=27482` / `release.kb=10886`（此前被增量构建的 28536 污染过）。

**留给后续的一条经验**：`performanceBaseline` 读的是增量构建产物，反复重建会让 debug 数字虚高近 1MB。判定体积回归必须先 `clean`，否则告警不可信。已写入 `AGENTS.md`。

### 顺带查出的既有问题：Readium 依赖完全未使用（不在本版处理）

排查 assets 构成时发现 `assets/readium/*` 占 1034.8 KB，含一个 414.6 KB 的 `divinaPlayer.js`——而 `CLAUDE.md` 写着「No WebView anywhere」。核实：

- `app/build.gradle.kts:124-127` 声明了 4 个 `org.readium.kotlin-toolkit:*:3.1.2`（shared / streamer / navigator / opds），引入于 `b5e4603`（v17）。
- **全仓源码零引用**：`grep -rn "org\.readium"` 在 `.kt`/`.xml` 中无任何命中，只有那 4 行依赖和 `proguard-rules.pro:82-83`。
- `proguard-rules.pro:82` 的 `-keep class org.readium.** { *; }` 让 R8 无法删除任何一个 Readium 类，**死代码被强行保留进发布包**。
- 那 1038 KB assets **已经进了正式 release APK**（28 个文件）——R8 只剥代码，不剥 assets，`-keep` 又挡住了代码剥离。
- 合并后 manifest 里 readium 命中数为 0，未引入组件或权限，**不是安全问题**，是纯体积浪费。

移除这 4 个依赖和那条 `-keep` 属于独立的清理项，会明显改变发布包体积，超出本版（配色 / 国际化 / 背景图）范围，**不在 v56.5.0 动手**，留作单独一版并需实测前后体积。

---

## 实施顺序

按「风险递增、每步可独立验证」排。前一步门禁不绿不进下一步。

1. **深色状态栏**（纯资源，无逻辑）→ 跑 `assembleDebug`。
2. **两个顶栏标题国际化**（`stats_title` 现成，`wheel_title` 需补 9 条）→ 跑 `verifyKotlinStyle` + `testDebugUnitTest`。
3. **阅读器主路径 11 处硬编码**（新增资源 × 9 语言）→ 同上。
4. **6 套新配色**（改 3 处，含那个硬编码的 `assertEquals(12, ...)`）→ 跑 `:core:testDebugUnitTest` 验对比度断言。
5. **背景图**（最大项，改 `ReadingSettings` + DataStore + 渲染 + 遮罩纯函数 + 配套测试）→ 全量六项门禁。

第 5 步若中途发现工作量超出单版合理范围，就停在第 4 步收版，背景图单独留给 v56.6.0。这个判断在实施到那一步、看清真实改动面之后再做，现在不预先承诺。

## 不做的事

- 不碰 `versionCode`/`versionName`，直到上述任务实际完成。
- 不推送，不合并，不发 Release。v56.4.4 的公开 Release 已经发出（`app-release.apk`，11,139,349 字节），下一次推 main 会再切一个新的公开版本。
